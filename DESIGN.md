# GitMini - 프로젝트 설계 문서

> **상태**: 설계 확정  
> **최종 수정**: 2026-02-03

---

## 1. 프로젝트 개요

### 한 줄 설명
> **초보자를 위한 미니멀 Git 클라이언트** - Git을 배우면서 쓰는 입문 도구

### 왜 만드는가? (Problem)

```
기존 도구의 문제:
├── GitHub Desktop: 기능이 많고 복잡함, 학습 목적에는 과함
├── 터미널 Git: 명령어 외워야 함, 초보자에게 어려움
└── VS Code Git: IDE에 종속됨

내가 원하는 것:
├── 간단한 Git 작업을 빠르게 (clone, commit, push, branch)
├── 여러 레포 상태를 한눈에
├── Git 명령어가 뭔지 보면서 학습
└── 브라우저 안 켜고 바로 사용
```

### 차별점 (A + B + C + D 조합)

| 차별점 | 설명 |
|--------|------|
| **A. Git 학습 도구** | 버튼 누르면 실행되는 Git 명령어 표시 → "이건 `git push origin main`이에요" |
| **B. 미니멀리즘** | GitHub Desktop의 20% 기능만. 복잡한 거 다 빼고 핵심만 |
| **C. 다중 레포 대시보드** | 여러 레포 상태를 한 화면에서 확인 |
| **D. 워크플로우 자동화** | 자주 쓰는 패턴을 원클릭으로 (Commit+Push 등) |

### GitHub Desktop과 비교

| 기능 | GitHub Desktop | GitMini |
|------|---------------|---------|
| Git 명령어 표시 | ❌ 숨김 | ✅ 실행 명령어 + 설명 |
| 다중 레포 대시보드 | ❌ 하나씩 | ✅ 한 화면에 여러 레포 |
| 복잡도 | 기능 많음 | 핵심 기능만 (미니멀) |
| 학습 목적 | ❌ | ✅ Git 배우면서 사용 |
| 원클릭 워크플로우 | 제한적 | ✅ Commit+Push 등 |

### 포트폴리오 어필 포인트

```
1. "실제로 내가 쓰려고 만든 도구" (실용성)
   └── 이 도구로 다른 프로젝트들을 관리했음 = 입증

2. "Git 학습자/초보자를 위한 교육적 기능" (차별화)
   └── GitHub Desktop과 다른 명확한 타겟

3. "JavaFX로 데스크톱 앱 개발 경험" (기술 스택)

4. "GitHub REST API 연동" (API 경험)

5. "UX 고려한 미니멀 디자인" (설계력)
   └── 불필요한 기능 제거 = 설계 결정의 증거
```

---

## 2. 기술 스택

| 구분 | 기술 | 선택 이유 |
|------|------|----------|
| 언어 | Java 17+ | 학습 목표, 크로스 플랫폼 |
| GUI | JavaFX + **FXML** | Java GUI 학습, UI/로직 분리 |
| UI 디자인 | Scene Builder | 시각적 UI 설계, FXML 생성 |
| Git 연동 | **ProcessBuilder** (git CLI) | Git 명령어 표시 기능과 시너지 |
| GitHub API | java.net.http.HttpClient | 표준 라이브러리, 추가 의존성 없음 |
| JSON | Gson | 가볍고 간단 |
| 빌드 | Gradle | 의존성 관리, jlink/jpackage 지원 |
| 패키징 | jpackage | 독립 실행파일(.exe) 생성 |

### 기술 선택 이유

#### FXML + Scene Builder 선택
```
장점:
├── UI와 로직 분리 → 유지보수 용이
├── Scene Builder로 시각적 디자인 가능
├── CSS로 스타일링 분리
└── 다른 프로젝트에서도 활용 가능한 스킬

학습 가치:
├── MVC 패턴 자연스럽게 적용
├── 선언적 UI 경험 (React/Flutter와 유사한 개념)
└── FXML ↔ Controller 바인딩 이해
```

#### ProcessBuilder (git CLI) 선택
```
선택 이유:
├── Git 명령어 표시 기능과 완벽한 시너지
│   └── 실제로 실행하는 명령어를 그대로 보여줌
├── 간단한 구현 (git 명령어 그대로 사용)
└── 학습 목적에 적합 (git 명령어 익히기)

전제 조건:
└── 사용자 PC에 git 설치 필요 (대부분 설치되어 있음)
```

---

## 3. 기능 명세

### 3.1 MVP (Must Have) - 핵심 기능

#### 레포지토리 관리
- [ ] 로컬 레포 목록 관리 (등록/제거)
- [ ] 레포 상태 표시 (변경 파일 수, 브랜치, 마지막 커밋)
- [ ] 새 레포 Clone (URL 입력 → 로컬에 클론)

#### 기본 Git 작업
- [ ] Stage (add) - 파일 선택 후 스테이징
- [ ] Commit - 메시지 입력 후 커밋
- [ ] Push - 원격에 푸시
- [ ] Pull - 원격에서 가져오기
- [ ] Fetch - 원격 상태 확인

#### 브랜치 (기본)
- [ ] 브랜치 목록 표시
- [ ] 브랜치 생성
- [ ] 브랜치 전환 (checkout/switch)

#### Git 명령어 표시 ⭐ (핵심 차별점)
- [ ] 모든 작업 시 실행되는 Git 명령어 표시
- [ ] 예: "Push" 버튼 → `git push origin main` 표시
- [ ] 명령어에 대한 간단한 설명 (툴팁 또는 패널)
- [ ] 명령어 히스토리 패널 (최근 실행한 명령어들)

#### 인증
- [ ] GitHub Personal Access Token 설정
- [ ] 토큰 로컬 저장 (암호화)

### 3.2 Should Have (MVP 완료 후)

- [ ] **원클릭 워크플로우** - Commit + Push 한번에
- [ ] **레포 생성** - GitHub에 새 레포 생성 (API)
- [ ] 커밋 히스토리 보기 (최근 N개)
- [ ] Diff 보기 (변경 내용 미리보기)
- [ ] 다크 모드

### 3.3 Nice to Have (시간 남으면)

- [ ] 윈도우 시작 프로그램 등록
- [ ] 커밋 알림 (장시간 커밋 안 했을 때)
- [ ] 간단한 통계 (오늘 커밋 수 등)
- [ ] 테마 커스터마이징

### 3.4 Won't Have (하지 않음)

- [ ] 코드 에디터
- [ ] **머지/리베이스** (복잡함, 터미널이나 IDE 사용)
- [ ] 충돌 해결 UI
- [ ] GitHub Actions 관리
- [ ] Issue/PR 관리 (웹으로 충분)
- [ ] 여러 GitHub 계정 지원

---

## 4. 화면 설계

### 4.1 메인 화면 (다중 레포 대시보드)

```
┌─────────────────────────────────────────────────────────────────┐
│  Git Dashboard                                    [설정] [최소화]│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📁 내 레포지토리                              [+ 레포 추가]     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📦 Algorithm_Drill          main     ✓ Clean            │   │
│  │    마지막 커밋: 2시간 전                    [열기]        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 📦 tycoon-lite              main     ⚠ 3 변경           │   │
│  │    마지막 커밋: 1일 전                      [열기]        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 📦 MyProject                develop  ⚠ 1 변경           │   │
│  │    마지막 커밋: 30분 전                     [열기]        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│  📜 최근 Git 명령어                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ > git status                                            │   │
│  │ > git add .                                             │   │
│  │ > git commit -m "feat: add new feature"                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 레포 상세 화면

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 뒤로    Algorithm_Drill                              [설정]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  브랜치: [main ▼]              원격: origin/main               │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 변경된 파일 (3)                                         │   │
│  │ ┌─────────────────────────────────────────────────────┐ │   │
│  │ │ ☑ README.md                              수정됨     │ │   │
│  │ │ ☑ src/Main.java                          수정됨     │ │   │
│  │ │ ☐ test.txt                               새 파일    │ │   │
│  │ └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  커밋 메시지: [                                            ]   │
│                                                                 │
│  [Stage 선택] [Commit] [Push] [Pull] [Fetch]                   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│  📜 실행된 명령어                                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ > git add README.md src/Main.java                       │   │
│  │   ℹ️ 선택한 파일을 스테이징 영역에 추가합니다            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 패키지 구조

```
GitMini/
├── build.gradle                          // Gradle 빌드 설정
├── settings.gradle
├── src/
│   └── main/
│       ├── java/
│       │   └── com/
│       │       └── gitmini/
│       │           ├── GitMiniApp.java           // JavaFX Application 진입점
│       │           ├── controller/
│       │           │   ├── MainController.java       // 메인 화면 (레포 목록)
│       │           │   ├── RepoDetailController.java // 레포 상세 (작업 화면)
│       │           │   └── SettingsController.java   // 설정 화면
│       │           ├── model/
│       │           │   ├── Repository.java           // 레포 정보 모델
│       │           │   ├── FileChange.java           // 변경 파일 정보
│       │           │   ├── GitCommand.java           // 실행된 명령어 정보
│       │           │   └── AppConfig.java            // 앱 설정 모델
│       │           ├── service/
│       │           │   ├── GitService.java           // Git 명령어 실행 (핵심)
│       │           │   ├── GitHubApiService.java     // GitHub REST API
│       │           │   └── ConfigService.java        // 설정 저장/로드
│       │           └── util/
│       │               ├── ProcessRunner.java        // ProcessBuilder 래퍼
│       │               ├── GitOutputParser.java      // Git 출력 파싱
│       │               └── TokenManager.java         // PAT 암호화 저장
│       └── resources/
│           ├── fxml/
│           │   ├── main.fxml                 // 메인 화면 레이아웃
│           │   ├── repo_detail.fxml          // 상세 화면 레이아웃
│           │   ├── settings.fxml             // 설정 화면
│           │   └── components/               // 재사용 컴포넌트
│           │       └── repo_card.fxml        // 레포 카드 컴포넌트
│           ├── css/
│           │   ├── style.css                 // 기본 스타일
│           │   └── dark.css                  // 다크 모드 (나중에)
│           └── images/
│               └── icon.png                  // 앱 아이콘
└── README.md
```

### 핵심 클래스 설명

| 클래스 | 역할 |
|--------|------|
| `GitMiniApp` | JavaFX Application 진입점, 메인 Stage 설정 |
| `GitService` | **핵심** - ProcessBuilder로 git 명령 실행, 명령어 기록 |
| `MainController` | 레포 목록 표시, 레포 추가/제거 |
| `RepoDetailController` | Stage/Commit/Push 등 작업, 명령어 히스토리 |
| `ProcessRunner` | 외부 프로세스 실행 유틸, 출력 캡처 |
| `GitOutputParser` | git status, git branch 등 출력 파싱 |

---

## 6. 개발 계획

### Phase 1: 환경 설정 + 기본 UI (1주)
- [ ] Gradle 프로젝트 설정
- [ ] JavaFX 환경 구성
- [ ] 메인 화면 레이아웃
- [ ] 레포 목록 표시 (하드코딩)

### Phase 2: Git 연동 (2주)
- [ ] ProcessBuilder로 git 명령어 실행
- [ ] git status 파싱
- [ ] Stage, Commit, Push 구현
- [ ] 명령어 히스토리 표시

### Phase 3: GitHub API (1주)
- [ ] PAT 인증
- [ ] Clone 기능
- [ ] (선택) 레포 생성

### Phase 4: 완성도 (1주)
- [ ] 설정 저장/로드
- [ ] 에러 핸들링
- [ ] UI 다듬기
- [ ] jpackage로 exe 빌드

### Phase 5: 확장 (선택)
- [ ] 다크 모드
- [ ] 원클릭 워크플로우
- [ ] 윈도우 시작 프로그램

---

## 7. 결정된 사항

| 항목 | 결정 | 이유 |
|------|------|------|
| 프로젝트 이름 | **GitMini** | 미니멀 강조 |
| Git 연동 방식 | **ProcessBuilder** (git CLI) | 명령어 표시 기능과 시너지 |
| UI 구현 방식 | **FXML + Scene Builder** | UI/로직 분리, 확장성, 학습 가치 |
| 차별점 방향 | **A+B+C+D** | 학습+미니멀+대시보드+워크플로우 |

### 미결정 사항 (개발 중 결정)

| 항목 | 옵션 | 비고 |
|------|------|------|
| 토큰 저장 방식 | 로컬 파일 암호화 vs OS Keychain | 일단 로컬 암호화로 시작 |
| 설정 파일 포맷 | JSON vs YAML | JSON 예상 (Gson 사용) |
| 창 크기/위치 저장 | 저장 vs 기본값 | 있으면 좋음 |

---

## 8. 참고 자료

- [JavaFX 공식 문서](https://openjfx.io/)
- [GitHub REST API](https://docs.github.com/en/rest)
- [JGit 문서](https://www.eclipse.org/jgit/)
- [jpackage 가이드](https://docs.oracle.com/en/java/javase/17/jpackage/)

---

## 문서 이력

| 날짜 | 내용 |
|------|------|
| 2026-02-03 | 초안 작성 |
| 2026-02-03 | 설계 확정 - 이름(GitMini), 기술스택(FXML), 차별점(A+B+C+D), 기능범위 |

