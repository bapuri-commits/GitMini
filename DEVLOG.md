# GitMini 개발 로그 (Development Log)

> **목적**: 프로젝트 진행 과정, 학습 내용, 결정 사항을 단계별로 기록  
> **형식**: 각 단계마다 목표 → 제안 → 실행 → 회고 → 다음 단계 순서로 기록

---

## Step 0: 프로젝트 초기화 및 Git 설정

**날짜**: 2026-02-05

### 목표
- GitMini 프로젝트의 Gradle 기본 골격 생성
- `.gitignore` 정리하여 GitHub 연동 준비
- 첫 커밋 생성

### AI 제안
1. **`.gitignore` 정리**
   - Gradle 빌드 산출물 (`.gradle/`, `build/`)
   - IDE 설정 파일 (`.idea/`, `*.iml` 등)
   - OS 파일 (`.DS_Store`, `Thumbs.db`)
   - 단, `gradle-wrapper.jar`, `gradle-wrapper.properties`는 버전 관리에 포함

2. **첫 커밋 메시지**
   - `chore: initialize GitMini Gradle project`

### 내가 한 행동
1. `.gitignore` 파일 확인 및 검토
2. `git add .` 실행
3. `git commit -m "chore: initialize GitMini Gradle Project"` 실행
4. `git log --oneline` 확인 → 커밋 해시: `9c764f5`

### 느낀 점 / 배운 점
- **`.gitignore`의 중요성**
  - IDE 설정 파일이나 빌드 산출물을 Git에 올리면 불필요한 충돌과 저장소 크기 증가가 발생
  - 프로젝트 초기 단계에서 `.gitignore`를 제대로 설정하는 것이 중요함

- **첫 커밋의 의미**
  - 프로젝트의 시작점을 명확히 기록
  - 나중에 "언제부터 시작했는지" 추적 가능

### 다음 예상 단계
- Java 17 toolchain 설정
- JavaFX / Gson 의존성 추가
- 패키지 구조 생성
- 기본 UI 뼈대 만들기

---

## Step 1: Java 17 Toolchain 설정

**날짜**: 2026-02-05

### 목표
- Gradle 수준에서 이 프로젝트의 공식 Java 버전을 17로 고정
- DESIGN.md의 "Java 17+" 기술 스택과 실제 빌드 설정 일치시키기

### AI 제안
1. **설정 위치**
   - `build.gradle` 파일에서 `plugins { id 'java' }` 블록 바로 아래에 추가

2. **추가할 코드**
   ```groovy
   java {
       toolchain {
           languageVersion = JavaLanguageVersion.of(17)
       }
   }
   ```

3. **설정 후 검증**
   - `./gradlew clean build` 실행하여 설정이 유효한지 확인

### 내가 한 행동
1. `build.gradle` 파일 열기
2. `plugins { id 'java' }` 블록 아래에 `java { toolchain { ... } }` 블록 추가
3. 파일 저장
4. (예정) `./gradlew clean build` 실행하여 검증

### 느낀 점 / 배운 점

#### 사용자 회고

**Q1. 왜 "Java 버전 설정"이 기능 구현보다 먼저 나와야 할까?**

> 당연히 기능을 구현하기 이전에 우리가 이런 버전에서 기능을 구현하고 있음을 알려야 가능한 부분과 한계점이 명확해지기 때문

**Q2. 만약 이 설정 없이 JavaFX/Gson 코딩을 먼저 했다면, 어떤 문제가 생길 수 있을까?**

> 설계단계에서 17로 하였으나 17에서 오히려 문제가 생기는 일이 생길수도 있다...

**Q3. `java.toolchain`과 예전식 `sourceCompatibility`/`targetCompatibility`의 차이는 무엇일까?**

> 위에 설명에서도 있었던 바와 같이 컴퓨터의 동작단계에서 강제적으로 권장 버전으로 컴파일하도록 만들 수 있다.

**Q4. 나중에 이 프로젝트를 다른 사람과 함께 개발한다면, 이 설정 덕분에 어떤 이득이 있을까?**

> 같은 자바 버전 환경에서 개발을 자연스럽게 진행하여 불필요한 호환과정을 거칠필요가 없다.

#### AI 완결 답변

**Q1에 대한 완결 답변:**
Java 버전 설정이 기능 구현보다 먼저 필요한 이유는 **"기준선 확립"** 때문입니다. 기능을 구현할 때 사용할 수 있는 API, 문법, 라이브러리 버전이 모두 Java 버전에 의존합니다. 예를 들어:
- Java 11 이전에는 `var` 키워드 사용 불가
- Java 17에서 도입된 Sealed Classes, Pattern Matching 등은 Java 11 환경에서는 컴파일 불가
- JavaFX 버전도 Java 버전과 호환성 문제가 있음

버전을 먼저 정하지 않으면, "내 컴퓨터에서는 되는데?" 같은 환경 의존적 버그가 발생하기 쉽습니다. 또한 IDE의 코드 검사, 자동 완성, 경고 메시지도 올바른 Java 버전 기준으로 작동해야 정확한 피드백을 받을 수 있습니다.

**Q2에 대한 완결 답변:**
설정 없이 먼저 코딩했다면 다음과 같은 문제들이 발생할 수 있습니다:

1. **환경별 불일치**
   - 개발자 A의 PC (Java 21): 새 문법 사용 가능 → 컴파일 성공
   - 개발자 B의 PC (Java 11): 같은 코드 → 컴파일 실패
   - 결과: "왜 내 컴퓨터에서는 안 되지?" 상황 발생

2. **라이브러리 호환성 문제**
   - JavaFX 17은 Java 17+ 필요
   - Java 11 환경에서 JavaFX 17 사용 시도 → 런타임 에러 가능
   - Gson도 특정 Java 버전 이상에서만 동작하는 기능이 있음

3. **설계 문서와 실제 코드의 불일치**
   - DESIGN.md: "Java 17+"
   - 실제 코드: Java 11에서도 돌아감 (또는 Java 21 전용 기능 사용)
   - 포트폴리오 설명 시 혼란 발생

4. **나중에 버전 고정 시 리팩토링 필요**
   - 이미 Java 21 전용 기능을 쓰고 있었는데, 나중에 Java 17로 고정하려면 코드 수정 필요
   - 초반에 고정하면 이런 리팩토링 비용 없음

**Q3에 대한 완결 답변:**
`sourceCompatibility`/`targetCompatibility`는 **"컴파일러에게 알려주는 힌트"** 수준입니다:
- `sourceCompatibility = '17'`: "Java 17 문법까지만 써라"
- `targetCompatibility = '17'`: "Java 17 바이트코드로 컴파일해라"

하지만 실제로 **어떤 JDK로 컴파일하는지는 개발자 PC의 JDK에 의존**합니다. Java 21 JDK로 컴파일해도 `targetCompatibility = '17'`이면 Java 17 바이트코드가 나오지만, 컴파일러 자체는 Java 21을 사용합니다.

반면 `java.toolchain`은 **"이 프로젝트는 Java 17 JDK를 사용해야 한다"**고 Gradle에게 명시적으로 요구합니다:
- Gradle이 Java 17 JDK를 찾아서 사용
- (고급 설정 시) 로컬에 Java 17이 없으면 자동으로 다운로드
- **컴파일러 자체가 Java 17 기준으로 동작**

즉, toolchain은 더 강력하고 명확한 "환경 고정" 메커니즘입니다. 특히 협업 환경에서는 "모든 개발자가 동일한 JDK 버전을 사용한다"는 것을 보장하는 데 유리합니다.

**Q4에 대한 완결 답변:**
협업 시 이 설정 덕분에 얻는 이득:

1. **환경 불일치 문제 해소**
   - 모든 개발자가 Java 17 기준으로 개발
   - "내 컴퓨터에서는 되는데?" 같은 문제 최소화

2. **온보딩 시간 단축**
   - 새 팀원이 프로젝트 클론 후 `build.gradle`만 보면 "아, Java 17이구나" 즉시 파악
   - 별도 문서나 README 없이도 환경 파악 가능

3. **CI/CD 파이프라인 일관성**
   - CI 서버도 같은 Java 17 환경으로 빌드/테스트
   - 로컬 환경과 CI 환경의 차이로 인한 빌드 실패 방지

4. **버전 업그레이드 계획 수립 용이**
   - "언제 Java 21로 올릴까?" 같은 결정을 프로젝트 전체 차원에서 논의 가능
   - 개별 개발자가 임의로 새 버전 기능을 쓰는 것 방지

5. **포트폴리오/이력서 작성 시 명확성**
   - "Java 17 기반 프로젝트"라고 자신 있게 설명 가능
   - 기술 스택이 설계 문서와 실제 코드에서 일치함을 증명

### 다음 예상 단계
- JavaFX / Gson 의존성 추가 (`build.gradle`의 `dependencies` 블록에 추가)
- 패키지 구조 생성 (`src/main/java/com/gitmini/...`, `src/main/resources/...`)
- 기본 UI 뼈대 만들기 (`GitMiniApp.java` + `main.fxml`)

---

## 형식 안내

각 Step은 다음 구조를 따릅니다:

- **목표**: 이 단계에서 달성하려는 것
- **AI 제안**: AI가 제시한 구체적인 방법/코드/설정
- **내가 한 행동**: 사용자가 실제로 수행한 작업
- **느낀 점 / 배운 점**: 회고 질문과 답변 (사용자 + AI 완결 답변)
- **다음 예상 단계**: 다음에 할 것으로 예상되는 작업

이 형식은 기본이며, 사용자의 언급을 통해 추가 필드나 구조를 확장할 수 있습니다.
